## Problem
The  list in  is used as a buffer for decoded frames. While frames are popped in , the  method can append multiple frames to the list.

## Why it Matters
For long-running RTSP streams, this can lead to significant memory pressure or crashes if the consumer lags behind the decoder, especially since  objects hold native memory.

## Location
- : Lines 80, 184, 188, 208

## Proposed Solution
Use a  with a  to bound the buffer size, or implement a more robust producer-consumer queue with backpressure.

**Severity**: critical
**Labels**: automated
